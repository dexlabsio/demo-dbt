name: dvd_rental
config-version: 2
version: 2.0
profile: 'dbt-postgres'
model-paths: ["models"]

models:
  dvd_rental:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    mart:
      +materialized: table 